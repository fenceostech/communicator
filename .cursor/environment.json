{
  "name": "FenceOS Console",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "next-dev",
      "command": ": \"${DATABASE_URL:=postgresql://fenceos:fenceos@localhost:5432/fenceos}\"; export DATABASE_URL; pnpm dev",
      "description": "Next.js dev server. Uses an injected DATABASE_URL (e.g. Supabase) when present, else falls back to the local PostgreSQL."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
